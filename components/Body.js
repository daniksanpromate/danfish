import { Text, StyleSheet } from 'react-native';
import { View } from 'react-native';
import { CameraView } from 'expo-camera';

export default function Body({ bodyStyle, bodyTextStyle }) {
  return (
    <View style={bodyStyle}>
      
    </View>
  );
}
